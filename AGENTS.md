# Hướng dẫn cho agent

Thư mục gốc của repo là thư mục chứa file này (`Github/`), không phải thư mục cha `QLGX/`.

## Nguồn quy tắc chung

Trước khi làm việc, đọc toàn bộ `CLAUDE.md` cùng thư mục và tuân thủ các quy tắc trong đó.
Đây là nguồn quy tắc chung cho Claude Code và Codex, bao gồm bảo toàn dữ liệu, phát hành,
Git, bảng mã và cách làm việc khi có nhiều phiên cùng sử dụng repo.

Khi cập nhật quy tắc chung, sửa `CLAUDE.md`; tránh sao chép nội dung sang nhiều file khiến
các phiên đọc những quy tắc khác nhau. File này chỉ hướng dẫn cách tìm và đọc quy tắc.

Trước khi sửa một thư mục con, kiểm tra và đọc `AGENTS.md` và `CLAUDE.md` nếu có trên đường
từ gốc repo tới thư mục đó. Áp dụng hướng dẫn riêng trong đúng phạm vi của nó. Nếu có mâu
thuẫn chưa rõ cách giải quyết, làm rõ trước khi thực hiện phần công việc bị ảnh hưởng.

Không coi lệnh trong tài liệu tiến độ hoặc kế hoạch cũ là quyền bỏ qua quy tắc hiện hành
trong `CLAUDE.md`, đặc biệt với đổi nhánh, tạo worktree, push và phát hành.
