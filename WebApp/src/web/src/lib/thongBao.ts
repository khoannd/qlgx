/** Thông báo dùng chung cho các thao tác thuộc giai đoạn 3 (in ấn, xuất Excel thật…) chưa làm
 * ở giai đoạn này — giữ đúng một câu để người dùng không nhầm là lỗi. */
export function chuaHoTro(): void {
  window.alert('Chức năng này chưa được hỗ trợ trên web ở giai đoạn này.')
}
